spring:
  application:
    name: front-api-service     # 和 Nacos 配置文件名保持一致
  cloud:
    nacos:
      config:
        namespace: d9f71636-9d41-462d-b6c2-aacd4a93a196
        server-addr: 47.237.23.152:8848
        username: nacos
        password: nacos321
        file-extension: yaml
        group: dev
        refresh-enabled: true   # 开启动态刷新
        # 添加文件扩展名配置
        prefix: ${spring.application.name}
        extension-configs:
          - data-id: front-api-service.yaml
            group: dev
            refresh: true
      discovery:
        enabled: false

